# 毕业设计管理系统 - 系统配置、安全认证与部署文档

## 一、项目概述

### 1.1 项目名称
毕业设计管理系统 (Diploma Project Management System) - 系统配置与部署部分

### 1.2 技术栈
- **运行环境**: JDK 17, MySQL 8.2, Node.js 18+
- **应用服务器**: Spring Boot 内嵌 Tomcat
- **前端构建**: Vite 5.x
- **版本控制**: Git + GitHub

### 1.3 开发分工
本部分负责系统级配置、安全认证机制及项目部署，包括：
- Spring Security 安全配置
- JWT Token 认证机制
- MyBatis-Plus 配置优化
- Swagger API 文档配置
- CORS 跨域处理
- AOP 操作日志切面
- 启动脚本与环境配置

---

## 二、系统配置文件详解

### 2.1 application.yml 主配置

```yaml
server:
  port: 8080                    # 服务端口

spring:
  output:
    ansi:
      enabled: always            # 控制台彩色输出
  
  datasource:                    # 数据源配置
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/diploma_project?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root               # 数据库用户名
    password: root               # 数据库密码
    hikari:                      # 连接池配置
      minimum-idle: 5             # 最小空闲连接
      maximum-pool-size: 20       # 最大连接数
      idle-timeout: 30000         # 空闲超时(ms)
      max-lifetime: 1800000       # 连接最大生命周期
      connection-timeout: 30000   # 连接超时(ms)

mybatis-plus:                     # MyBatis-Plus配置
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.internship.entity
  configuration:
    map-underscore-to-camel-case: true    # 下划线转驼峰
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  # SQL日志

# JWT配置
jwt:
  secret: diploma-project-secret-key-2026-very-long-secret-for-security!
  expiration: 86400000              # Token有效期(24小时)

# 文件上传配置
file:
  upload-path: ./uploads/          # 上传根目录
  max-size: 10485760              # 最大文件大小(10MB)
  allowed-types: pdf,doc,docx,xls,xlsx,ppt,pptx,zip,rar,jpg,png,gif
```

### 2.2 日志配置 (logback-spring.xml)

```xml
<configuration>
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- 文件输出 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/diploma-system.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/diploma-system.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder><pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern></encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
</configuration>
```

---

## 三、Spring Security 安全配置

### 3.1 SecurityConfig.java 核心代码

```java
@Configuration
@EnableWebSecurity
@RequiredArgsConstructor
public class SecurityConfig {
    
    private final JwtAuthenticationFilter jwtAuthenticationFilter;
    
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            // 禁用CSRF（前后端分离不需要）
            .csrf(csrf -> csrf.disable())
            
            // 禁用Session（使用JWT无状态认证）
            .sessionManagement(session -> 
                session.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
            
            // 异常处理
            .exceptionHandling(exception -> exception
                .authenticationEntryPoint((request, response, authException) -> {
                    response.setStatus(401);
                    response.setContentType("application/json;charset=UTF-8");
                    response.getWriter().write("{\"code\":401,\"message\":\"未认证，请先登录\",\"data\":null}");
                })
                .accessDeniedHandler((request, response, accessDeniedException) -> {
                    response.setStatus(403);
                    response.setContentType("application/json;charset=UTF-8");
                    response.getWriter().write("{\"code\":403,\"message\":\"无权限访问\",\"data\":null}");
                })
            )
            
            // URL授权规则
            .authorizeHttpRequests(auth -> auth
                // 公开接口（无需Token）
                .requestMatchers(
                    "/api/auth/**",           // 登录/注册/切换角色
                    "/api/v1/auth/login",
                    "/api/v1/auth/register",
                    "/api/v1/test/**",
                    "/api/v1/files/download/**",
                    "/api/v1/files/templates/**",
                    "/doc.html",              // Swagger文档
                    "/swagger-ui.html",
                    "/swagger-ui/**",
                    "/v3/api-docs/**",
                    "/webjars/**",
                    "/favicon.ico"
                ).permitAll()
                
                // 其他所有请求需要认证
                .anyRequest().authenticated()
            )
            
            // 添加JWT过滤器
            .addFilterBefore(jwtAuthenticationFilter, UsernamePasswordAuthenticationFilter.class)
            
            // CORS跨域配置
            .cors(cors -> cors.configurationSource(request -> {
                CorsConfiguration config = new CorsConfiguration();
                config.setAllowedOriginPatterns(List.of("*"));
                config.setAllowedMethods(List.of("GET", "POST", "PUT", "DELETE", "OPTIONS"));
                config.setAllowedHeaders(List.of("*"));
                config.setAllowCredentials(true);
                config.setMaxAge(3600L);
                return config;
            }));
        
        return http.build();
    }
    
    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

### 3.2 权限控制注解说明

在Controller中使用 `@PreAuthorize` 注解控制访问：

| 注解 | 允许的角色 |
|------|-----------|
| `hasRole('ADMIN')` | 仅校级管理员 |
| `hasAnyRole('college_admin', 'major_admin')` | 学院或专业管理员 |
| `hasRole('teacher')` | 指导教师 |
| `hasRole('student')` | 学生 |
| 无注解 | 所有已登录用户 |

**示例:**
```java
@PostMapping
@PreAuthorize("hasAnyRole('college_admin', 'major_admin')")
public Result<ProjectBatch> createBatch(@RequestBody ProjectBatch batch) { ... }
```

---

## 四、JWT 认证机制

### 4.1 JwtUtil.java 工具类核心方法

```java
@Component
@Slf4j
public class JwtUtil {
    
    @Value("${jwt.secret}")
    private String secret;
    
    @Value("${jwt.expiration}")
    private Long expiration;
    
    /**
     * 生成JWT Token
     * @param userId 用户ID
     * @param username 用户名
     * @param role 角色
     * @return Token字符串
     */
    public String generateToken(Long userId, String username, String role) {
        Date now = new Date();
        Date expiryDate = new Date(now.getTime() + expiration);
        
        return Jwts.builder()
                .setSubject(String.valueOf(userId))
                .claim("username", username)
                .claim("role", role)
                .setIssuedAt(now)
                .setExpiration(expiryDate)
                .signWith(SignatureAlgorithm.HS256, secret.getBytes())
                .compact();
    }
    
    /**
     * 解析Token获取用户ID
     */
    public Long getUserIdFromToken(String token) {
        Claims claims = parseToken(token);
        return Long.parseLong(claims.getSubject());
    }
    
    /**
     * 验证Token是否有效
     */
    public boolean validateToken(String token) {
        try {
            parseToken(token);
            return !isTokenExpired(token);
        } catch (Exception e) {
            return false;
        }
    }
    
    /**
     * 从请求上下文获取当前用户ID
     */
    public Long getCurrentUserId() {
        Authentication auth = SecurityContextHolder.getContext().getAuthentication();
        if (auth != null && auth.isAuthenticated()) {
            String name = auth.getName();
            if (name != null && !name.equals("anonymousUser")) {
                return Long.parseLong(name);
            }
        }
        return null;
    }
}
```

### 4.2 JwtAuthenticationFilter.java 过滤器

```java
@Component
@RequiredArgsConstructor
public class JwtAuthenticationFilter extends OncePerRequestFilter {
    
    private final JwtUtil jwtUtil;
    
    @Override
    protected void doFilterInternal(HttpServletRequest request,
                                    HttpServletResponse response,
                                    FilterChain chain) throws ServletException, IOException {
        final String authHeader = request.getHeader("Authorization");
        
        if (authHeader != null && authHeader.startsWith("Bearer ")) {
            final String token = authHeader.substring(7);  // 去掉"Bearer "
            
            if (jwtUtil.validateToken(token)) {
                Long userId = jwtUtil.getUserIdFromToken(token);
                
                // 创建认证对象并存入SecurityContext
                UsernamePasswordAuthenticationToken authentication = 
                    new UsernamePasswordAuthenticationToken(
                        String.valueOf(userId), 
                        null, 
                        AuthorityUtils.createAuthorityList(jwtUtil.getRoleCodeFromToken(token))
                    );
                
                authentication.setDetails(new WebAuthenticationDetailsSource().buildDetails(request));
                SecurityContextHolder.getContext().setAuthentication(authentication);
            }
        }
        
        chain.doFilter(request, response);
    }
}
```

### 4.3 认证流程图

```
用户登录 → 验证账号密码 → 生成JWT Token
      ↓
前端存储Token(localStorage)
      ↓
后续请求携带Header: Authorization: Bearer xxx
      ↓
JwtAuthenticationFilter拦截请求
      ↓
解析验证Token有效性
      ↓
有效 → 设置SecurityContext → 放行
无效 → 返回401未认证
```

---

## 五、MyBatis-Plus 配置

### 5.1 MyBatisPlusConfig.java

```java
@Configuration
@MapperScan("com.internship.mapper")
public class MyBatisPlusConfig {
    
    /**
     * 分页插件配置
     */
    @Bean
    public MybatisPlusInterceptor mybatisPlusInterceptor() {
        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();
        
        // 分页插件
        interceptor.addInnerInterceptor(PaginationInnerInterceptor(DbType.MYSQL));
        
        // 乐观锁插件(可选)
        // interceptor.addInnerInterceptor(OptimisticLockerInnerPlugin());
        
        return interceptor;
    }
}
```

### 5.2 MyBatisMetaObjectHandler.java 自动填充

```java
@Component
public class MyBatisMetaObjectHandler implements MetaObjectHandler {
    
    @Override
    public void insertFill(MetaObject metaObject) {
        this.strictInsertFill(metaObject, "createdAt", LocalDateTime.class, LocalDateTime.now());
        this.strictInsertFill(metaObject, "updatedAt", LocalDateTime.class, LocalDateTime.now());
    }
    
    @Override
    public void updateFill(MetaObject metaObject) {
        this.strictUpdateFill(metaObject, "updatedAt", LocalDateTime.class, LocalDateTime.now());
    }
}
```

**实体类中使用:**
```java
@TableField(fill = FieldFill.INSERT)
private LocalDateTime createdAt;

@TableField(fill = FieldFill.INSERT_UPDATE)
private LocalDateTime updatedAt;
```

---

## 六、Swagger API 文档配置

### 6.1 SwaggerConfig.java

```java
@Configuration
public class SwaggerConfig {
    
    @Bean
    public OpenAPI customOpenAPI() {
        return new OpenAPI()
                .info(new Info()
                        .title("毕业设计管理系统 API")
                        .version("1.0.0")
                        .description("RESTful API文档\n\n**功能模块:**\n- 用户认证\n- 批次管理\n- 题库管理\n- 选题流程\n- 任务书/中期/答辩/终期\n- 成绩计算"))
                        .contact(new Contact()
                                .name("毕设系统开发组")
                                .email("support@example.edu.cn")))
                .externalDocs(new ExternalDocumentation()
                        .description("项目GitHub仓库")
                        .url("https://github.com/ordinarydestiny/diploma-project"));
    }
    
    @Bean
    public GroupedOpenApi customApi() {
        return GroupedOpenApi.builder()
                .group("毕设管理")
                .pathsToMatch("/api/**")
                .build();
    }
}
```

### 6.2 Controller 注解示例

```java
@RestController
@RequestMapping("/api/topics")
@Tag(name = "题库管理", description = "毕设题目CRUD操作")
public class TopicController {
    
    @GetMapping
    @Operation(summary = "题目列表(分页)")
    @Parameter(name = "page", description = "页码")
    @Parameter(name = "size", description = "每页数量")
    public Result<IPage<Topic>> listTopics(...) { ... }
    
    @PostMapping
    @Operation(summary = "新增题目")
    @PreAuthorize("hasAnyRole('teacher', 'major_admin')")
    @LogOperation("新增题目")  // 自定义操作日志注解
    public Result<Topic> createTopic(@RequestBody Topic topic) { ... }
}
```

**访问地址**: http://localhost:8080/doc.html

---

## 七、AOP 操作日志切面

### 7.1 LogOperation.java 自定义注解

```java
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface LogOperation {
    String value() default "";  // 操作描述
}
```

### 7.2 OperationLogAspect.java 切面实现

```java
@Component
@Aspect
@RequiredArgsConstructor
public class OperationLogAspect {
    
    private final OperationLogMapper operationLogMapper;
    private final JwtUtil jwtUtil;
    
    @Around("@annotation(com.internship.annotation.LogOperation)")
    public Object around(ProceedingJoinPoint joinPoint) throws Throwable {
        MethodSignature signature = (MethodSignature) joinPoint.getSignature();
        LogOperation logAnnotation = signature.getMethod().getAnnotation(LogOperation.class);
        
        OperationLog operationLog = new OperationLog();
        operationLog.setUserId(jwtUtil.getCurrentUserId());
        operationLog.setUserRole(jwtUtil.getCurrentRole());
        operationLog.setOperationType(getOperationType(joinPoint));
        operationLog.setTargetTable(getTableName(joinPoint));
        operationLog.setDescription(logAnnotation.value());
        operationLog.setIpAddress(getClientIp());
        operationLog.setUserAgent(getUserAgent());
        operationLog.setRequestMethod(getRequestMethod());
        operationLog.setRequestUrl(getRequestUrl());
        operationLog.setCreatedAt(LocalDateTime.now());
        
        Object result = null;
        try {
            result = joinPoint.proceed();  // 执行目标方法
            return result;
        } finally {
            operationLogMapper.insert(operationLog);  // 记录日志
        }
    }
}
```

---

## 八、CORS 跨域配置

### 8.1 CorsConfig.java

```java
@Configuration
public class CorsConfig {
    
    @Bean
    public CorsFilter corsFilter() {
        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
        CorsConfiguration config = new CorsConfiguration();
        
        config.setAllowCredentials(true);           // 允许携带Cookie
        config.addAllowedOriginPattern("*");       // 允许所有来源(生产环境应限制)
        config.addAllowedHeader("*");              // 允许所有请求头
        config.addAllowedMethod("*");              // 允许所有HTTP方法
        
        source.registerCorsConfiguration("/api/**", config);
        
        return new CorsFilter(source);
    }
}
```

### 8.2 Vite 开发代理配置

```javascript
// vite.config.js
export default defineConfig({
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, '/api')
      }
    }
  }
})
```

---

## 九、启动脚本

### 9.1 start.bat (Windows)

```batch
@echo off
chcp 65001 >nul
echo ============================================
echo   毕业设计管理系统 - 后端服务启动脚本
echo ============================================

set JAVA_HOME=D:\java17
set PATH=%JAVA_HOME%\bin;%PATH%

echo [INFO] 切换终端编码为 UTF-8...
echo [INFO] 启动 Spring Boot 服务...

"%JAVA_HOME%\bin\java" -Dfile.encoding=UTF-8 -jar target\internship-system-1.0.0.jar

pause
```

### 9.2 start.ps1 (PowerShell)

```powershell
# 设置Java环境
$env:JAVA_HOME = "D:\java17"
$env:Path = "$env:JAVA_HOME\bin;$env:Path"

# 启动服务
Write-Host "============================================"
Write-Host "  毕业设计管理系统 - 后端服务"
Write-Host "============================================"

& java -Dfile.encoding=UTF-8 -jar target/internship-system-1.0.0.jar
```

### 9.3 package.json 脚本

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  }
}
```

---

## 十、部署指南

### 10.1 环境要求

| 组件 | 版本要求 |
|------|----------|
| JDK | 17+ |
| MySQL | 8.0+ |
| Node.js | 18+ (仅开发环境) |
| Maven | 3.6+ (后端构建) |
| npm/pnpm | 9+ (前端构建) |

### 10.2 生产环境部署步骤

#### 1. 数据库准备
```bash
# 创建数据库
mysql -u root -p -e "CREATE DATABASE diploma_project DEFAULT CHARSET utf8mb4;"

# 导入表结构和初始数据
mysql -u root -p diploma_project < src/sql/diploma_project.sql
```

#### 2. 后端部署
```bash
# 进入后端目录
cd src/backend

# 修改生产环境配置
# 编辑 src/main/resources/application.yml:
#   - 修改数据库连接信息
#   - 修改jwt.secret为强密钥
#   - 修改file.upload-path为实际路径

# 打包
mvn clean package -DskipTests

# 启动
nohup java -jar target/internship-system-1.0.0.jar --server.port=8080 > app.log 2>&1 &
```

#### 3. 前端部署
```bash
# 进入前端目录
cd src/frontend

# 安装依赖
npm install

# 构建生产版本
npm run build

# 将dist目录部署到Nginx
cp -r dist/* /var/www/html/
```

#### 4. Nginx 反向代理配置
```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    location / {
        root /var/www/html;
        try_files $uri $uri/ /index.html;
    }
    
    location /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

### 10.3 Docker 部署 (可选)

```dockerfile
# Dockerfile
FROM eclipse-temurin:17-jre
WORKDIR /app
COPY target/internship-system-1.0.0.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
```

```yaml
# docker-compose.yml
version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://db:3306/diploma_project
    depends_on:
      - db
      
  db:
    image: mysql:8.2
    environment:
      MYSQL_ROOT_PASSWORD: your_password
      MYSQL_DATABASE: diploma_project
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3306:3306"

volumes:
  mysql_data:
```

---

## 十一、完成的工作清单

### 11.1 安全配置
- [x] Spring Security 安全框架集成
- [x] JWT Token 认证机制
- [x] 密码BCrypt加密
- [x] 角色权限控制 (@PreAuthorize)
- [x] CORS 跨域处理
- [x] CSRF 防护禁用(前后端分离)

### 11.2 框架配置
- [x] MyBatis-Plus 分页插件
- [x] MyBatis-Plus 自动填充
- [x] Swagger/Knife4j API文档
- [x] 全局异常处理器
- [x] 统一响应格式 Result<T>

### 11.3 辅助功能
- [x] AOP 操作日志切面
- [x] JWT 工具类
- [x] 日志配置(logback)
- [x] 文件上传配置

### 11.4 部署相关
- [x] Windows启动脚本 (start.bat)
- [x] PowerShell启动脚本 (start.ps1)
- [x] Vite代理配置
- [x] 生产环境部署指南
- [x] Docker部署方案

---

## 十二、技术亮点总结

1. **无状态JWT认证**: 不依赖Session，适合分布式部署
2. **细粒度权限控制**: 方法级别的角色权限校验
3. **AOP操作日志**: 自动记录敏感操作，无需手动编写日志代码
4. **统一异常处理**: 友好的错误响应格式，便于前端处理
5. **完整的API文档**: Knife4j增强版Swagger，支持在线调试
6. **灵活的配置体系**: YAML配置 + 环境变量支持

---

*文档版本: v1.0*
*最后更新: 2026-06-02*
*作者: 系统配置与部署组*